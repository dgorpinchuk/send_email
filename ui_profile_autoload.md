# SMTP profile auto-load

Selecting an SMTP profile in the Profile dropdown immediately loads its saved username, password, server and port into the corresponding fields. Saving the profile updates those values for the selected profile.
